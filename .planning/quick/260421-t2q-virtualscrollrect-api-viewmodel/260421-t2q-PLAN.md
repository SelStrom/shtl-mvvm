---
phase: quick
plan: 260421-t2q
type: execute
wave: 1
depends_on: []
files_modified:
  - Runtime/Core/VirtualScroll/VirtualScrollRect.cs
  - Runtime/AssemblyInfo.cs
autonomous: true

must_haves:
  truths:
    - "VirtualScrollRect не экспортирует публичных свойств/методов, кроме интерфейсных (IBeginDragHandler и т.д.) и самого класса"
    - "VirtualCollectionBinding продолжает работать без изменений (тот же assembly)"
    - "Тесты продолжают компилироваться и проходить через InternalsVisibleTo"
  artifacts:
    - path: "Runtime/Core/VirtualScroll/VirtualScrollRect.cs"
      provides: "VirtualScrollRect с internal API"
      contains: "internal float ScrollPosition"
    - path: "Runtime/AssemblyInfo.cs"
      provides: "InternalsVisibleTo для тестовых сборок"
      contains: "InternalsVisibleTo"
  key_links:
    - from: "Runtime/AssemblyInfo.cs"
      to: "Tests/Runtime/Shtl.Mvvm.Tests.Runtime.asmdef"
      via: "InternalsVisibleTo(\"Shtl.Mvvm.Tests.Runtime\")"
      pattern: "InternalsVisibleTo.*Shtl\\.Mvvm\\.Tests\\.Runtime"
    - from: "Runtime/AssemblyInfo.cs"
      to: "Tests/Runtime/Shtl.Mvvm.Tests.asmdef"
      via: "InternalsVisibleTo(\"Shtl.Mvvm.Tests\")"
      pattern: "InternalsVisibleTo.*Shtl\\.Mvvm\\.Tests"
---

<objective>
Скрыть публичное API VirtualScrollRect, оставив класс "максимально тупым" -- только отрисовка ViewModel.

Цель: VirtualScrollRect не должен экспортировать свойства и методы наружу. Все ScrollPosition, ViewportHeight, OverscanCount, SetContentHeight, ScrollTo, ScrollToIndex, SetOnScrollPositionChanged, ResetScroll, Velocity становятся internal. Enum MovementType тоже internal. Класс остаётся public (MonoBehaviour в сценах). Интерфейсные методы (OnBeginDrag и т.д.) остаются public по требованию Unity.

Результат: VirtualScrollRect.cs с internal API + Runtime/AssemblyInfo.cs с InternalsVisibleTo для тестов.
</objective>

<execution_context>
@.planning/STATE.md
</execution_context>

<context>
@Runtime/Core/VirtualScroll/VirtualScrollRect.cs
@Runtime/Core/Bindings/VirtualCollectionBinding.cs
@Tests/Runtime/VirtualCollectionBindingTests.cs
@Runtime/Utils/VirtualListBindExtensions.cs

Текущие сборки:
- Runtime: `Shtl.Mvvm` (Runtime/Shtl.Mvvm.asmdef)
- Тесты (Editor): `Shtl.Mvvm.Tests` (Tests/Runtime/Shtl.Mvvm.Tests.asmdef)
- Тесты (Runtime): `Shtl.Mvvm.Tests.Runtime` (Tests/Runtime/Shtl.Mvvm.Tests.Runtime.asmdef)

VirtualCollectionBinding и VirtualListBindExtensions находятся в той же runtime-сборке `Shtl.Mvvm` -- им internal-доступ даётся автоматически. Изменений в этих файлах не требуется.

Тесты используют ScrollPosition напрямую -- нужен InternalsVisibleTo.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать AssemblyInfo.cs с InternalsVisibleTo</name>
  <files>Runtime/AssemblyInfo.cs</files>
  <action>
Создать файл `Runtime/AssemblyInfo.cs` со следующим содержимым:

```csharp
using System.Runtime.CompilerServices;

[assembly: InternalsVisibleTo("Shtl.Mvvm.Tests")]
[assembly: InternalsVisibleTo("Shtl.Mvvm.Tests.Runtime")]
```

Имена сборок берутся из asmdef-файлов:
- `Shtl.Mvvm.Tests` -- Tests/Runtime/Shtl.Mvvm.Tests.asmdef (Editor-тесты)
- `Shtl.Mvvm.Tests.Runtime` -- Tests/Runtime/Shtl.Mvvm.Tests.Runtime.asmdef (Runtime-тесты)

Этот файл ДОЛЖЕН быть создан ДО изменения модификаторов доступа, чтобы тесты не теряли доступ к internal-членам.
  </action>
  <verify>
    <automated>test -f Runtime/AssemblyInfo.cs && grep -q "InternalsVisibleTo" Runtime/AssemblyInfo.cs && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Файл Runtime/AssemblyInfo.cs существует и содержит InternalsVisibleTo для обоих тестовых assembly.</done>
</task>

<task type="auto">
  <name>Task 2: Изменить модификаторы доступа в VirtualScrollRect.cs</name>
  <files>Runtime/Core/VirtualScroll/VirtualScrollRect.cs</files>
  <action>
В файле `Runtime/Core/VirtualScroll/VirtualScrollRect.cs` заменить `public` на `internal` для следующих членов:

1. **Enum MovementType** (строка 9): `public enum MovementType` -> `internal enum MovementType`

2. **ScrollPosition** (строка 37-46): `public float ScrollPosition` -> `internal float ScrollPosition`

3. **Velocity** (строка 48): `public float Velocity` -> `internal float Velocity`

4. **OverscanCount** (строка 50-54): `public int OverscanCount` -> `internal int OverscanCount`

5. **ViewportHeight** (строка 56): `public float ViewportHeight` -> `internal float ViewportHeight`

6. **SetContentHeight** (строка 58): `public void SetContentHeight` -> `internal void SetContentHeight`

7. **ScrollTo** (строка 66): `public void ScrollTo` -> `internal void ScrollTo`

8. **ScrollToIndex** (строка 73): `public void ScrollToIndex` -> `internal void ScrollToIndex`

9. **SetOnScrollPositionChanged** (строка 84): `public void SetOnScrollPositionChanged` -> `internal void SetOnScrollPositionChanged`

10. **ResetScroll** (строка 89): `public void ResetScroll` -> `internal void ResetScroll`

НЕ менять:
- `public class VirtualScrollRect` -- MonoBehaviour, ссылается из сцен и из VirtualListBindExtensions (публичный API пакета)
- `public void OnBeginDrag` -- реализация IBeginDragHandler
- `public void OnDrag` -- реализация IDragHandler
- `public void OnEndDrag` -- реализация IEndDragHandler
- `public void OnScroll` -- реализация IScrollHandler

Никаких других изменений в файле. Не менять логику, не менять форматирование.
  </action>
  <verify>
    <automated>cd /Users/selstrom/work/projects/shtl-mvvm && grep -c "public" Runtime/Core/VirtualScroll/VirtualScrollRect.cs | xargs -I{} test {} -eq 5 && echo "PASS: exactly 5 public members (class + 4 interface methods)" || echo "FAIL: expected 5 public members"</automated>
  </verify>
  <done>
VirtualScrollRect.cs содержит ровно 5 вхождений `public`:
1. `public class VirtualScrollRect`
2. `public void OnBeginDrag`
3. `public void OnDrag`
4. `public void OnEndDrag`
5. `public void OnScroll`

Все остальные свойства и методы имеют модификатор `internal`. Enum MovementType имеет модификатор `internal`.
  </done>
</task>

</tasks>

<verification>
1. Файл `Runtime/AssemblyInfo.cs` существует с корректными `InternalsVisibleTo`
2. В `VirtualScrollRect.cs` ровно 5 `public` членов (класс + 4 интерфейсных метода)
3. `VirtualCollectionBinding.cs` и `VirtualListBindExtensions.cs` НЕ изменены (проверить git diff)
4. `VirtualCollectionBindingTests.cs` НЕ изменён (проверить git diff)
</verification>

<success_criteria>
- VirtualScrollRect не экспортирует публичных свойств и методов, кроме интерфейсных реализаций
- MovementType enum -- internal
- Тестовые сборки имеют доступ к internal-членам через InternalsVisibleTo
- Существующий код VirtualCollectionBinding и тесты работают без изменений
</success_criteria>

<output>
После завершения создать коммит с сообщением:
`refactor: hide VirtualScrollRect public API behind internal`
</output>
