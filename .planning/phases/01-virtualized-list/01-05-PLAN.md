---
phase: 01-virtualized-list
plan: 05
type: execute
wave: 5
depends_on: [01-04]
files_modified:
  - Samples~/Sample/Packages/manifest.json
autonomous: false
requirements: [TEST-04]
gap_closure: true
gaps_addressed:
  - gap-1-tests-not-discovered

must_haves:
  truths:
    - "Unity Test Runner в Sample-проекте показывает тест-сьюты ReactiveVirtualListTests, LayoutCalculatorTests, ViewRecyclingPoolTests, VirtualCollectionBindingTests"
    - "Run All в Test Runner завершается зелёным — все 49 тестов проходят"
  artifacts:
    - path: "Samples~/Sample/Packages/manifest.json"
      provides: "testables opt-in для com.shtl.mvvm"
      contains: "\"testables\""
  key_links:
    - from: "Samples~/Sample/Packages/manifest.json"
      to: "Tests/Runtime/Shtl.Mvvm.Tests.Runtime.asmdef"
      via: "UPM testables field → UNITY_INCLUDE_TESTS defineConstraint активируется"
      pattern: "testables.*com\\.shtl\\.mvvm"
---

<objective>
Исправить отсутствие дискавери тестов в Unity Test Runner Sample-проекта.

Purpose: Unity Test Runner не видит тест-сьюты пакета com.shtl.mvvm, подключённого через file:-ссылку, без явного opt-in поля testables в manifest.json потребляющего проекта. defineConstraint UNITY_INCLUDE_TESTS в asmdef не активируется без этой записи.

Output: Одна строка в manifest.json → все 49 тестов фазы 1 становятся видимы и зелены в Test Runner.
</objective>

<execution_context>
@/Users/selstrom/.claude/get-shit-done/workflows/execute-plan.md
@/Users/selstrom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-virtualized-list/01-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Добавить testables в manifest.json Sample-проекта</name>
  <files>Samples~/Sample/Packages/manifest.json</files>
  <action>
Прочитать файл Samples~/Sample/Packages/manifest.json.

Добавить поле "testables" на верхний уровень JSON-объекта (рядом с "dependencies"), со значением массива, содержащего имя пакета:

```json
{
  "dependencies": { ... },
  "testables": [
    "com.shtl.mvvm"
  ]
}
```

Поле добавить ПОСЛЕ блока dependencies, перед закрывающей скобкой объекта. Соблюдать существующее форматирование файла (2 пробела отступ, LF).

Правила JSON:
- Добавить запятую после закрывающей скобки dependencies-блока
- "testables" — массив строк, не объект
- Имя пакета строго "com.shtl.mvvm" (совпадает с package.json в корне репо)

Сохранить файл.
  </action>
  <verify>grep -c "testables" Samples~/Sample/Packages/manifest.json</verify>
  <done>Команда возвращает 1. Файл содержит поле testables с com.shtl.mvvm.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Добавлено поле testables в manifest.json. Unity автоматически перечитает манифест при следующем фокусе на Editor окне.
  </what-built>
  <how-to-verify>
    1. Переключить фокус в Unity Editor (кликнуть на окно Editor) — Unity перечитает manifest.json и выполнит domain reload
    2. Если домен не обновился автоматически: Assets → Reimport All или удалить папку Library/PackageCache/com.shtl.mvvm@* вручную через Finder
    3. Открыть Window → General → Test Runner
    4. Переключиться на вкладку EditMode
    5. Убедиться, что видны тест-сьюты:
       - ReactiveVirtualListTests (13 тестов)
       - LayoutCalculatorTests (19 тестов)
       - ViewRecyclingPoolTests (7 тестов)
       - VirtualCollectionBindingTests (10 тестов)
    6. Нажать "Run All"
    7. Все тесты должны показать зелёную галочку (✓)
  </how-to-verify>
  <resume-signal>Напиши "approved" если все 49 тестов зелёные, или опиши проблему</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest.json → UPM resolver | Файл читается Unity Package Manager при импорте; данные идут от разработчика, не из сети |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-01 | Tampering | manifest.json | accept | Файл в репо, изменения трекаются git; только разработчик с доступом к репо может изменить |
</threat_model>

<verification>
```bash
grep "testables" Samples~/Sample/Packages/manifest.json
grep "com.shtl.mvvm" Samples~/Sample/Packages/manifest.json
```
Обе команды должны вернуть непустой результат.
</verification>

<success_criteria>
- Поле testables с com.shtl.mvvm присутствует в manifest.json
- Unity Test Runner показывает все 4 тест-сьюта фазы 1
- Run All завершается зелёным (49/49)
</success_criteria>

<output>
После завершения создать `.planning/phases/01-virtualized-list/01-05-SUMMARY.md`
</output>
